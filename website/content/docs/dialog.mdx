<script lang="ts">
 import ToggleDemo from "$lib/ui/toggledemo.svelte";
 import { getDemo } from "$lib/codemos";

</script>

# Dialog 

A UI element that displays a modal window with content, mostly used for alerts, confirmations etc. 

## Usage
<ToggleDemo demo={getDemo("dialog", "usage")} />
